# Marketing static assets

This folder is for optional marketing images (logos, screenshots, etc.) served from `/marketing/...`.

The **marketing home hero** is rendered with **WebGL (Three.js) on all viewports**; there is no required static hero image for phones.
